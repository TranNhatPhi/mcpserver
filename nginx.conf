events {
    worker_connections 1024;
}

http {
    upstream mcp_backend {
        least_conn;
        server mcp:8000;
        # Thêm instance khi scale: server mcp2:8000;
        keepalive 32;
    }

    # HTTP -> HTTPS redirect + certbot challenge
    server {
        listen 80;
        server_name 47-128-154-64.sslip.io;

        location /.well-known/acme-challenge/ {
            root /var/www/certbot;
        }

        location / {
            return 301 https://$host$request_uri;
        }
    }

    # HTTPS
    server {
        listen 443 ssl;
        server_name 47-128-154-64.sslip.io;

        ssl_certificate     /etc/letsencrypt/live/47-128-154-64.sslip.io/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/47-128-154-64.sslip.io/privkey.pem;
        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_ciphers         HIGH:!aNULL:!MD5;

        # Tăng timeout cho MCP long-polling
        proxy_read_timeout  300s;
        proxy_send_timeout  300s;
        proxy_connect_timeout 10s;

        location / {
            proxy_pass         http://mcp_backend;
            proxy_http_version 1.1;
            proxy_set_header   Upgrade $http_upgrade;
            proxy_set_header   Connection "upgrade";
            proxy_set_header   Host $host;
            proxy_set_header   X-Real-IP $remote_addr;
            proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto $scheme;
            proxy_buffering    off;
        }
    }
}
